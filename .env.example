NODE_ENV=development
PORT=3000
# Generate a unique random value with at least 32 characters for production.
JWT_SECRET=replace-with-a-long-random-secret
DB_FILE=./server/ecard.sqlite
# Required in production; must match the browser origin.
APP_ORIGIN=http://localhost:3000
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=school@example.com
